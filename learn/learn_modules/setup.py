from setuptools import setup

setup(name='text_analyzer',
			version='0.0.1',
			description='Example for learing purposes',
			author='Jose',
			packages=['text_analyzer'],
			install_requires=['matplotlib',
								'pycodestyle']
)